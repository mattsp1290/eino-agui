// Package stream provides the reusable eino stream to AG-UI event tap.
//
// If live tool-call streaming is enabled, callers must not also emit post-turn
// tool proposals for the same calls.
package stream
